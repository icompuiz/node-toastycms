/**
 * Defines the main routes in the application.
 * The routes you see here will be anchors '#/' unless specifically configured otherwise.
 */

define(['./module'], function (inputFormats) {
    'use strict';

    return inputFormats.config([function() {}]);
});
